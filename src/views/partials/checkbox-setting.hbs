<div
  data-render-condition="{{render-condition}}"
  class="ta-x-checkbox
    {{conditional (object ta-x-hide=render-condition)}}
    {{classes}}"
  style="{{style}}"
>
  <label>
    {{label}}
  </label>
  <div class="frm-grp frm-tgl">
    <input
      type="checkbox"
      id="{{id}}"
      name="{{ternary name name id}}"
      data-config-path="{{config-path}}"
      checked=""
    />

    <label for="{{ternary name name id}}">
    </label>
  </div>
  {{#if helper-text}}
    <p
      data-render-condition="{{helper-render-condition}}"
      class="ta-x-settings-helper-text
        {{conditional (object ta-x-hide=helper-render-condition)}}"
    >
      {{helper-text}}
    </p>
  {{/if}}
</div>